'use strict';

let resultdir = './3/download/insta_result'

let name = resultdir.slice(resultdir.lastIndexOf('/')+1)+'.zip'
console.log(name)
//console.log(str.slice(0,index) +'_result' + str.slice(index))
//console.dir((str.match(new RegExp("/","g")) || []).length)

